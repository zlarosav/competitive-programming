#include <bits/stdc++.h>

using namespace std;

int main() {
  // Para C++ 11 no hay una función de gcd
  cout << "😭" << endl;

  // C++ 14, C++ 17, en adelante
  cout << __gcd(30, 48) << endl; 

  // Desde C++ 17 en adelante
  cout << gcd(30, 48) << endl;

  return 0;
}