#include <bits/stdc++.h>

using namespace std;

void solve() {
  int x; cin >> x;
  if (x == 1) {
    cout << -1; return;
  }
  cout << 2; 
}

int main() {
  ios_base::sync_with_stdio(0);
  cin.tie(0);

  int t = 1; 
  // cin >> t;
  while (t--) {
    solve();
  }

  return 0;
}