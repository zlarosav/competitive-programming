#include <bits/stdc++.h>

using namespace std;

/* void solve() {
  while (true) {
    int n; cin >> n;
    if (n == 0) break;
    
    vector<int> B(n);
    for (int i = 0; i < n; ++i) {
      cin >> B[i];
    }
    
    vector<int> A(n);
    A[0] = B[0];
    for (int i = 1; i < n; ++i) {
      A[i] = B[i - 1] ^ B[i];
    }

    for (int i = 0; i < n; ++i) {
      cout << A[i] << " ";
    }
    cout << endl;
  }
} */

void solve() {
  
}

int main() {
  ios_base::sync_with_stdio(0);
  cin.tie(0);

  int t = 1;
  // cin >> t;
  while (t--) {
    solve();
  }

  return 0;
}

// https://youtu.be/S1nXr88YxIo?si=X2bPfwSEdrvTk7Ct&t=1434